### AJAX
* 方法
```
function Ajax(type, url, data, succeed, failed){
  var xhr = new XMLHttpRequest();
  xhr.responseType = 'json';

  if(typeof data == "object"){
    var str = '';
    for(var key in data){
        str += key + '=' + data[key] +'&';
    }
    data = str.replace(/&$/, '');

    if(type.toUpperCase() == "GET"){
      url += '?' + data;
      xhr.open("GET", url, true);
      xhr.send();
    }else{
      xhr.open("POST", url, true);
      xhr.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
      xhr.send(data);
    }
  }

  xhr.onreadystatechange = function(){
    if(xhr.readyState == 4){
      if(xhr.status == 200){
        succeed(xhr.response);
      }else{
        if(failed) failed(xhr.status);
      }
    }
  };
}
```
* 使用
```
Ajax('post', 'ajax.php', {name:'foo', sex:'male'}, function(response){
  console.log(response);
}, function(error){
  console.log(error);
});
```

### Timer
* 延时
    * window.setTimeout();
    * window.clearTimeout();
```
var foo = function(){ console.log(123); };
var timer = setTimeout("foo();", 1000);
clearTimeout(timer);
```
* 循环
    * window.setInterval();
    * window.clearInterval();
```
var timer = setInterval(function(){
  console.log(123);
}, 1000);
clearInterval(timer);
```

